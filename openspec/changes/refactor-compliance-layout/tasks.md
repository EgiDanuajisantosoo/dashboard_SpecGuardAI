## 1. UI Layout Refactor

- [x] 1.1 Implement Tailwind CSS grid wrapper in `compliance.blade.php` around the main content sections.
- [x] 1.2 Assign the "Workflow Compliance Map" to the left column (`lg:col-span-8`).
- [x] 1.3 Create a vertical stack container for the right column (`lg:col-span-4`).

## 2. Component Reordering

- [x] 2.1 Move the "Rekomendasi Prompt AI" (AI Fix Prompt) block to the top of the right column.
- [x] 2.2 Move the "Recent Commits" block below the AI Fix Prompt in the right column.
- [x] 2.3 Move the "Feature Status" panel to the bottom of the right column.

## 3. Polish & Verification

- [x] 3.1 Adjust spacing and borders between the two columns for a cohesive look.
- [x] 3.2 Verify that the layout correctly stacks vertically on mobile/small screens.
- [x] 3.3 Ensure Mermaid diagram interactivity (zoom, pan, dynamic coloring) remains intact within the narrower container.
